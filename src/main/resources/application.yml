server:
  port: 8080

spring:
  application:
    name: user-zelda
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/user_zelda?serverTimezone=UTC&characterEncoding=UTF-8
    username: ENC(xatt/xbf5ohUTNnpnGxxiQ==)
    password: ENC(Hg6tzU1uYkC5KlUumpLVBigXYOkM9x00)
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false
    show-sql: true
    hibernate:
      format_sql: true
      ddl-auto: validate
  zipkin:
    service:
      name: user-zelda

